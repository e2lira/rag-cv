# Reauditoría — RFC-0003 / PR #68

## VEREDICTO: PASS

El gate ADU queda satisfecho. No se encontraron bloqueantes ni mayores pendientes.

## Validación de correcciones

| Criterio | Evidencia | Resultado |
|---|---|---|
| CA-6 | `48eab1d` añade una consulta léxica sin coincidencia con el umbral por defecto `0.016`; HEAD con CI verde | Cumple |
| CA-10 | `2a18e50` rompe deliberadamente el invariante p95 ≤ 250 ms y deja `integration-linux` en rojo; `afb2205` lo restaura con ambos jobs verdes | Cumple |
| Informe CA-10 | La descripción de la PR declara el par reparador y sustituye expresamente la justificación anterior de `98dc39d` | Cumple |

## Evidencia de CI

- HEAD remoto `48eab1d`: `unit-windows` e `integration-linux` en verde.
- `2a18e50`: `integration-linux` en fallo, como exige la transición roja.
- `afb2205`: ambos jobs en verde.

## Decisión

La regresión deliberada de CA-10 satisface RFC-0014 §6.2.2: la única invariante del criterio es el p95 ≤ 250 ms; `time.sleep(0.3)` la viola en la misma aserción, y el commit siguiente la restaura. La norma exige romper cada invariante, no un mecanismo de implementación concreto.

La PR puede avanzar al merge.

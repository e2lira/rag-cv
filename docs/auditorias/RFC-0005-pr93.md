# Informe de auditoría — RFC-0005 · PR #93

- **PR:** [#93](https://github.com/e2lira/rag-cv/pull/93)
- **HEAD auditado:** `649d18f41cf18e5824642fda60fead9470568af5`
- **Fecha:** 2026-08-24
- **Veredicto:** **PASS con advertencia**

## Correcciones verificadas

1. **A-17 — claves de producción en pruebas:** corregido. No quedan coincidencias
   `rcv_live_` en `tests/`. La secuencia `0fe5aa0` (CI roja) → `68c102a` (CI verde)
   demuestra el ciclo de reparación.
2. **A-18 — secretos AWS en `app/`:** corregido. La búsqueda requerida
   `secretsmanager|boto3|API_KEYS_SECRET_ID` no arroja resultados en `app/`.
   La guarda estructural fue trasladada a `invoke lint`, donde corresponde.
   `8eab509` falla y `ef846ac` pasa en `unit-windows` e `integration-linux`.
3. **RFC-0014 §7 — I/O en prueba `unit`:** corregido. La comprobación que recorría el
   árbol fue retirada de la suite `unit` en `649d18f`; el SHA final pasa ambos jobs CI.

## Comprobaciones ADU

- **TDD-1/TDD-2:** los ciclos correctivos nuevos tienen evidencia CI asociada por SHA.
- **P-9:** no se observó una prueba modificada dentro del mismo commit verde de producción
  que ocultara un fallo del ciclo correctivo.
- **Gates A-17 y A-18:** comprobados directamente sobre el HEAD actual y cubiertos por
  la tarea estructural `invoke lint`.

## Advertencia

**ADR-0017 no está versionado en el HEAD.** `app/main.py`, `app/services/chat.py` y los
commits de la implementación de `AgentFactory` lo citan, pero no existe
`docs/adr/ADR-0017*` en el árbol `649d18f`. No bloquea los gates cerrados de RFC-0005,
pero debe incorporarse antes de usar esa decisión como contrato arquitectónico trazable.

## Conclusión

No quedan hallazgos Bloqueantes ni Mayores dentro del gate ADU de RFC-0005. El desarrollador
puede continuar, con la advertencia documental indicada.

# Informe de auditoría — RFC-0020 · PR #95

- **PR:** [#95](https://github.com/e2lira/rag-cv/pull/95)
- **HEAD auditado:** `5e82e304e297c6869293f6d805283fcd8a17560c`
- **Fecha:** 2026-08-24
- **Veredicto:** **PASS con advertencias**

## Correcciones verificadas

1. **TDD-2:** reparado. Los pares de prueba/implementación tienen CI propio rojo→verde;
   se verificaron `328b9e9`→`df8bb5c`, `4365ff7`→`6ad06cb` y
   `d30ea20`→`03cd4c2` por SHA.
2. **TDD-3:** reparado. El guard descarta comentarios de línea completa antes de buscar
   directivas efectivas.
3. **CA-12, retención e Informe de Implementación:** incorporados. El HEAD tiene también
   el job `auditoria-de-dependencias` en verde.
4. **CA-17 y CA-18:** reparados. La rotación vive bajo `$RAG_CV_HOME/logs/` y se
   programa en el `crontab` del operador; detectar `NOPASSWD` ahora ejecuta `exit 1`.
   El ciclo `e0bb48d` (rojo) → `5e82e30` (verde) tiene checks propios por SHA.

## Advertencias

1. **Runbook contradice la rotación implementada** — `deploy/README.md:53` aún indica
   instalar `/etc/logrotate.d/rag-cv`, mientras el artefacto correcto usa
   `$RAG_CV_HOME/logs/logrotate.conf`. Podría llevar al operador a reinstalar la variante
   que RFC-0019 §7 prohíbe.

2. **El README promete verificar CI sin que el script lo haga** —
   `deploy/README.md:112` afirma que `deploy.sh` verifica CI verde; el script sólo hace
   `git rev-parse --verify` y no consulta checks. Corregir el texto o implementar la
   comprobación prometida.

## Conclusión

No quedan hallazgos Bloqueantes ni Mayores. El desarrollador puede continuar; las dos
advertencias documentales deben corregirse antes de ejecutar el despliegue en el VPS.
